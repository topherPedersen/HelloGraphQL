import logo from './logo.svg';
import './App.css';
import {
  createBrowserRouter,
  RouterProvider,
} from "react-router-dom";

function App() {
  return (
    <div>
      {/* TODO: Is App.js still used after installing React Router? Delete if no longer needed. */}
      <h1>KGQL: GraphQL Radio</h1>
    </div>
  );
}
